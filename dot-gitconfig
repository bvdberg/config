[user]
	name = Bas van den Berg
	email = bas.van.den.berg.nl@gmail.com
[alias]
    st = status
    cm = checkout master
    po = remote prune origin
    cc = clean -f -d
    co = checkout
    mm = merge master
    ss = diff --shortstat
    wd = diff --color-words
    lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr)%C(bold blue)<%an>%Creset' --abbrev-commit
    tags = log --tags --simplify-by-decoration --pretty='format:%Cred%h%Creset %ai %d %C(bold blue)<%an>%Creset'
[core]
    editor = vim
[color]
	diff = auto
	status = auto
	branch = auto
[http]
	sslVerify = false
[push]
	default = tracking
